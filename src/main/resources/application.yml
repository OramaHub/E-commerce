spring:
  application:
    name: e-commerce
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  datasource:
    url: ${POSTGRES_HOST}
    username: ${POSTGRES_USERNAME}
    password: ${POSTGRES_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

server:
  port: 8081

jwt:
  secret: ${JWT_SECRET}
  access-expiration: ${ACCESS_EXPIRATION:3600000}
  refresh-expiration: ${REFRESH_EXPIRATION:2592000000}

admin:
  email: ${ADMIN_EMAIL}
  password: ${ADMIN_PASSWORD}
  name: ${ADMIN_NAME}
  cpf: ${ADMIN_CPF}
  phone: ${ADMIN_PHONE}

stripe:
  secret-key: ${STRIPE_SECRET_KEY}
  success-url: ${STRIPE_SUCCESS_URL:http://localhost:5173/checkout/success}
  cancel-url: ${STRIPE_CANCEL_URL:http://localhost:5173/checkout/cancel}

rate-limit:
  default-requests-per-minute: 60
  endpoints:
    "POST:/api/auth/login":
      requests-per-minute: 10
    "POST:/api/auth/register":
      requests-per-minute: 5
    "POST:/api/orders":
      requests-per-minute: 30
